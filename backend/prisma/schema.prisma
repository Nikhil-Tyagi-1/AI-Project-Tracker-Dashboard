// Prisma schema for AI Project Tracker Pro
// Models (Project, Task, …) will be added in the data-model milestone.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
