/**
 * Shared domain types re-exported from constants for feature modules.
 * Prefer importing enum values from `@/constants` and types from here or constants.
 */
export type {
  Priority,
  ProjectStatus,
  SelectOption,
  TaskStatus,
} from "@/constants/enums";
